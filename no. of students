class Solution(object):
    def busyStudent(self, startTime, endTime, queryTime):
        c=0
        for z in range(len(startTime)):
            if startTime[z]<=queryTime and endTime[z]>=queryTime:
                c+=1
        return c    
        
